DEVICE="eth0"
BOOTPROTO="dhcp"
ONBOOT="yes"
TYPE="Ethernet"
PERSISTENT_DHCLIENT="yes"

# Prevent the DNS to be modified by the DHCP server (Or we can't resolve Red Hat services from OS1):
PEERDNS=no
DNS1=209.132.186.218
